# Bot token — found under OAuth & Permissions after installing the app to your workspace
SLACK_BOT_TOKEN=xoxb-your-bot-token

# App-level token — create one under Basic Information > App-Level Tokens
# with the connections:write scope (required for Socket Mode)
SLACK_APP_TOKEN=xapp-your-app-level-token
